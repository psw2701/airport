<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.psw.mapper.ReplyMapper">
<select id="list" resultType="ReplyVO">
		select * from reply where bno = #{bNo.no}
		order by no desc
	</select>
	<insert id="create">
		insert into reply (no, managerNo, content)
		values(#{no}, #{managerNo.name}, #{content})
	</insert>
	<update id="update">
		update tbl_reply set content = #{content}, title = #{title}
		where no = #{no}
	</update>
	<delete id="delete">
		delete from reply where no = #{no}
	</delete>
	<select id="listPage" resultType="ReplyVO">
		select * from reply
		where bno=#{bNo}
		order by no desc
		limit #{cri.pageStart}, #{cri.perPageNum}
	</select>
	<select id="totalCount" resultType="int">
		select count(bno)
		from reply
		where bno=#{bNo.no}
	</select>
	
	<select id="selectByRno" resultType="ReplyVO">
		select * from reply 
		where no = #{no}
	</select>
	
	
</mapper>